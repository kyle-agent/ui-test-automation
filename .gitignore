# 사람이 만든 로그인 세션 (쿠키/토큰)과 로그인 브라우저 프로필. 절대 커밋하지 않는다.
recordings/session-*.json
recordings/profile-*/
recordings/**/screenshots/
recordings/**/*.jpg
recordings/**/*.png

# 메뉴 API 원본 응답 (정규화본 snapshots/ 만 커밋)
service-map/raw/

# Node / Playwright
node_modules/
test-results/
playwright-report/
blob-report/

# Python (uv)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# 로컬 설정
.env
.env.local
